name: my_app
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  calendar_timeline: ^0.7.1

  cupertino_icons: ^1.0.2

  flutter_swiper: 1.1.6

dev_dependencies:
  flutter_native_splash: ^1.2.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

flutter_native_splash:
  color: "#00BFFE"
  image: assets/splash.png
  fullscreen: true
  # background_image: assets/splashBack.png
  # android_gravity: center_horizontal, center_vertical
  # ios_content_mode: center_horizontal, center_vertical

  # 다크모드 일 경우
  # color_dark:
